<h1>New Address type</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.address_type_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.address_type_path(@conn, :index) %></span>
