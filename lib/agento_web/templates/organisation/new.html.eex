<h1>New Organisation</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.organisation_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.organisation_path(@conn, :index) %></span>
